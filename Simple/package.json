{
  "name": "Simple",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "preinstall": "cd .. && npm i && npm run lint && npm run build",
    "postinstall": "metro-with-symlinks",
    "run:android": "cd android && ./gradlew clean && cd .. && react-native run-android",
    "run:debug": "cd android && ./gradlew clean && ./gradlew assembleDebug",
    "reset:packager": "watchman watch-del-all && node_modules/react-native/scripts/packager.sh --reset-cache",
    "start:packager": "react-native start",
    "build:release": ":",
    "generate:android:signing-key": "keytool -genkey -v -keystore simple.keystore -alias simple -keyalg RSA -keysize 2048 -validity 10000 && mv simple.keystore android/app",
    "generate:android:apk": "npm run generate:android:bundle && cd android && ./gradlew assembleRelease",
    "generate:android:bundle": "npm run build:release && react-native bundle --platform android --dev false --entry-file index.js --bundle-output ./android/app/src/main/assets/index.android.bundle --assets-dest ./android/app/src/main/res/"
  },
  "dependencies": {
    "react": "16.5.1",
    "react-native": "0.57.1",
    "react-native-color-matrix-image-filters": "4.0.0",
    "react-native-fs": "^2.12.1",
    "react-native-image-filter-kit": "file:../",
    "react-native-view-shot": "^2.5.0",
    "rn-fetch-blob": "^0.10.13"
  },
  "devDependencies": {
    "metro-react-native-babel-preset": "0.45.2",
    "metro-with-symlinks": "^1.3.0",
    "babel-core": "6.26.3",
    "fable-splitter": "0.1.21"
  },
  "jest": {
    "preset": "react-native"
  }
}
